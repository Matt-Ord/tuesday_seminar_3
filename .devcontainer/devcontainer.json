{
    "name": "Debian",
    "image": "mcr.microsoft.com/devcontainers/base:bookworm",
    "features": {
        "ghcr.io/devcontainers/features/rust:1": {
            "version": "latest",
            "profile": "default"
        },
        "ghcr.io/devcontainers/features/node:1": {
            "version": "lts"
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "editor.formatOnSave": true,
                "editor.codeActionsOnSave": {
                    "source.fixAll": "always"
                },
                "tinymist.lint.enabled": true
            },
            "extensions": [
                "myriad-dreamin.tinymist",
                "astral-sh.ty",
                "charliermarsh.ruff",
                "streetsidesoftware.code-spell-checker"
            ]
        }
    },
    "onCreateCommand": "npm install -g cspell && sudo apt-get update && sudo apt-get install fontconfig pkg-config -y && cargo install --locked typst-cli && sudo mkdir -p /usr/share/fonts/custom && sudo cp fonts/* /usr/share/fonts/custom/ && sudo fc-cache -fv"
}